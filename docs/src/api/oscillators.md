# Oscillators

`ScQubitsMimic` includes lightweight oscillator models that share the same
spectral interface as the qubit and circuit types.

```@docs
Oscillator
KerrOscillator
annihilation_operator
creation_operator
number_operator
```
